@model IEnumerable<InternalBookingApp.DTOs.Resource.ResourceDto>
@{
    ViewData["Title"] = "Resources";
}

<div class="container-fluid">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2><i class="bi bi-building"></i> Resources</h2>
        <a asp-action="Create" class="btn btn-primary">
            <i class="bi bi-plus-circle"></i> Add Resource
        </a>
    </div>

    <div class="card">
        <div class="card-body">
            @if (Model.Any())
            {
                <div class="table-responsive">
                    <table class="table table-striped table-hover">
                        <thead class="table-dark">
                            <tr>
                                <th style="width:10%">Name</th>
                                <th style="width:20%">Description</th>
                                <th style="width:10%">Location</th>
                                <th style="width:10%">Capacity</th>
                                <th style="width:10%">Status</th>
                                <th style="width:12%">Actions</th>
                        </thead>
                        <tbody>
                            @foreach (var resource in Model)
                            {
                                <tr>
                                    <td>@resource.Name</td>
                                    <td>@resource.Description</td>
                                    <td>@resource.Location</td>
                                    <td>@resource.Capacity</td>
                                    <td>
                                        <span class="badge @(resource.IsAvailable ? "bg-success" : "bg-danger")">
                                            @(resource.IsAvailable ? "Available" : "Unavailable")
                                        </span>
                                    </td>
                                    <td>
                                        <a asp-action="Edit" asp-route-id="@resource.Id" class="btn btn-sm btn-outline-primary">
                                            <i class="bi bi-pencil"></i>
                                        </a>
                                        <a asp-action="Delete" asp-route-id="@resource.Id"
                                            class="btn btn-sm btn-outline-danger">
                                            <i class="bi bi-trash"></i>
                                        </a>
                                        <a asp-action="Details" asp-route-id="@resource.Id"
                                            class="btn btn-sm btn-outline-warning">
                                            <i class="bi bi-eye"></i> Details
                                        </a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            }
            else
            {
                <div class="alert alert-info">No resources found</div>
            }
        </div>
    </div>
</div>